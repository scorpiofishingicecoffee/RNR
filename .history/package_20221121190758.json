{
  "name": "heroku-deploy",
  "description": "Build scripts for Heroku",
  "engines": {
    "node": "16.16.0"
  },
  "scripts":{
          "clean": "rm -rf public",
          "build":"npm install --prefix client && npm run build --prefix client",
          "
  }
}
